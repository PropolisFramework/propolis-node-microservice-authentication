/**
 * @file
 * @description
 * Actions:
 * - Test successful login
 * - Test unsuccessful login
 */

require('../index');

/**
 * @description Login then test API result and DB logs
 */
test('Successful login', () => {

});

/**
 * @description No-login then test API result and DB logs
 */
test('Unsuccessful login (wrong username)', () => {

});

/**
 * @description No-Login then test API result and DB logs
 */
test('Unsuccessful login (wrong password)', () => {

});

/**
 * @description No-Login then test API result and DB logs
 */
test('Unsuccessful login (wrong username and password)', () => {

});