/**
 * @file
 * @description
 * Actions:
 * - Automatic timeout sign-out
 * - Simple sign-out
 * - Simple sign-out from a none login state
 */

require('../index');

/**
 * @description With small timeout for automatic sign-out
 * - Login -> tmp timeout -> sign-out
 */
test('Automatic timeout sign-out', () => {

});

/**
 * @description Simple call to sign-out
 * - Login -> sign-out
 */
test('Simple sign-out', () => {

});

/**
 * @description Simple sign-out from a none login state
 * - sign-out
 */
test('Simple sign-out from a none login state', () => {

});