require('../index');

test('Change username (username already used)', () => {
    expect().toBe(false);
});

test('Change username (username has wrong characters)', () => {

});

test('Change username (username too short)', () => {

});

test('Change username (username too long)', () => {

});